<%= form_for(well) do |f| %>
  <% if well.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(well.errors.count, "error") %> prohibited this well from being saved:</h2>

      <ul>
      <% well.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
